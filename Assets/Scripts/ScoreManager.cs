using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreManager : MonoBehaviour
{
    public static int hostages, hostagesDeadByMonster = 0, hostagesDeadByYou = 0, hostagesSaved = 0, score = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
